---
# Source: env0-agent/templates/persistent-volume-claim.yml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: env0-state-volume-claim
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: "env0-state-sc"
  
  resources:
    requests:
      storage: 300Gi
